import { LinkView } from '@/shared/ui';

export enum HowTrustScoreCountsButtonViewType {
  Tooltip = LinkView.Tooltip,
  Button = LinkView.Button,
}

export enum HowTrustScoreCountsInfoViewType {
  Tooltip = 'tooltip',
  Modal = 'modal',
}
